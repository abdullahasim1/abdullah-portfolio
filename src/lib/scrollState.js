/* R3F useFrame ke andar React state nahi padha jaata —
   is liye scroll progress ek mutable shared store mein rakhte hain */
export const scrollState = {
  /* #home section ka scroll progress 0 → 1 */
  hero: { value: 0 },
};
